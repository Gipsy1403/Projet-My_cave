{% extends 'base.html.twig' %}

{% block linkfavicon %}
	<link rel="icon" type="image/png" href="{{asset("/img/faviconexplorer.png")}}" sizes="96x96" />
{% endblock %}

{% block title %}Catalogue{% endblock %}
{% block banner %}{% endblock %}


{% block body %}

<section>
	{# <div class="filter">
		<h2>Filtres</h2>
		<a href={{path('filtres' , {'filter': 'ASC'} )}}>Noms Vins de A-Z</a>
		<a href={{path('filtres' , {'filter': 'DESC'} )}}>Noms Vins de Z-A</a>
		<div class="filter">
		{{form_start(filtreform)}} #}
			{# {{ form_widget(filtreform) }}
			<button>Filtrer</button>
		{{form_end(filtreform)}}
    </div> #}

	<article>
		<div class="container_explorer">
		{% for bottle in bottles %}
			<div class="card_description">
				<img class="card_wine" src={{asset('images/' ~ bottle.imageName )}} alt="Photographie d'une bouteille de vin">
				<div class="card_explorer">
					<h3>{{bottle.name}}</h3>
					<h4>{{bottle.year}}</h4>
					<p> {% for grape in bottle.grapes %}
							{{ grape.name }}{% if not loop.last %}, {% endif %}
						{% endfor %}
					</p>
					<p class="region_card">{{ bottle.region.name }}</p>
				</div>
				<div class="description_item">
					<p>{{bottle.description}}</p>
				</div>

				{# {% if bottle.cellar != null %}
					{% if bottle.id not in bottle.cellar %}
						{% if bottle.cellar.id == app.cellar.id %} #}
					{# <button class="btn_add_cellar"><a href={{path ("addbottlecellar",{"ids":bottle.id, "id":cellar.id})}}>Ajouter à ma Cave</a></button> #}
						{# {% endif %} #}
					{# {% endif %} #}
				{# {% endif %} #}
					{# <button class="btn_add_cellar"><a href={{path ("addbottlecellar",{"ids":bottle.id})}}>Ajouter à ma Cave</a></button> #}
				{# ancien bloc mais gardée au cas où #}
					<button class="btn_add_cellar"><a  href={{path ("addbottlecellar",{"id":bottle.id})}}>Ajouter à ma Cave</a></button>
				
			</div>	
		{% endfor %}
		</div>
	</article>
</section>
{% endblock %}
